const PageNotFound = () => (
  <div className="container">
    <div
      className="d-flex justify-content-center align-items-center alert alert-danger"
      role="alert"
    >
      This page does not exist <span style={{ fontSize: "2rem" }}>😕</span>
    </div>
  </div>
);

export default PageNotFound;